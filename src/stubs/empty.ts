// Leere Stub-Datei fuer Lovable/TanStack-Start Pakete
// Diese werden im SPA-Mode nicht gebraucht
export default {};
export const createServerFn = () => () => {};
export const createMiddleware = () => () => {};
export const json = (data) => data;
export const redirect = (url) => ({ url });
export const useServerFn = (fn) => fn;
